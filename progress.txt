# In DO Time v2 Foundation — Progress Log

## Project: In DO Time v2 (Foundation)
Started: 2026-05-14

## Goal of This Ralph Run

Reset the schema and code around "trails" (replacing `clients`) and add a `rates` ledger.
End state: a working v2-shape time tracker. NO journal, observations, or briefing yet.

## Pre-existing Learnings (from v1 + design conversation)

- This is a Next.js 16 + Supabase + Tailwind 4 app. Single-user, no auth. RLS is on but permissive.
- Supabase client is accessed exclusively via `getSupabase()` from `src/lib/supabase.ts`. Don't create new clients.
- Demo mode is real and important — `NEXT_PUBLIC_DEMO_MODE=true` swaps to a mock client in `src/lib/supabaseDemo.ts`. Both code paths must work end-to-end. If a query is added in real Supabase, the demo client must handle it too.
- Types flow from `src/types/database.ts`. It is the single source of truth for table shapes. Update this FIRST when changing schema.
- v1 schema had `clients` / `projects` / `time_entries`. v2 schema has `trails` / `projects` / `rates` / `time_entries`. The `projects` table is re-parented from `client_id` to `trail_id`.
- Migration is DESTRUCTIVE — drop v1 tables, create v2 tables. Davis has confirmed no production data to preserve. Don't try to migrate data across.
- Ralph WRITES the migration SQL but does NOT execute it. Davis runs migrations manually in Supabase SQL Editor after this run completes.
- Rates are time-versioned. A rate row is immutable once inserted. To "change" a rate, insert a new row with new `effective_from` AND update the previous row's `effective_until`.
- The trail `kind` enum is exactly: `client`, `employer`, `personal`, `project`. No others.
- Time entries no longer carry rate. Cost is computed at query/display time via `src/lib/rates.ts`.
- `time_entries.notes` is now semantically "invoice line description only" — not a general capture field. Capture will live in observations (run #2).
- `time_entries.source_observation_id` is a UUID column with NO FK constraint yet. The `observations` table doesn't exist until run #2.

## Patterns to Preserve

- Brand: DO Code Lab logo, "In DO Time" wordmark, green palette (`#3A7D44` primary).
- Typography: Playfair Display (headings), Inter (body).
- Spacing: 8px grid. Radius: 8px (buttons/inputs), 12px (cards).
- Theme: light/dark via `next-themes`. ThemeProvider/ThemeToggle/ThemeWatcher components are unchanged.
- Timer engine in `src/lib/timer.ts` is the only place timer operations live. Don't duplicate this logic elsewhere.

## Anti-Patterns (Things Ralph Should NOT Do)

- Do NOT try to preserve v1 data through the schema reset. The migration is destructive by design.
- Do NOT create a separate `clients` concept alongside trails. Clients are just trails with `kind='client'`.
- Do NOT add a rate column to `time_entries`. Rates live in the `rates` table only.
- Do NOT UPDATE a rate row in place. Always insert a new one.
- Do NOT refactor unrelated code "while you're in there." Stay focused on the task list.
- Do NOT build journal, observation, briefing, or open-loop features in this run. They are explicitly out of scope.
- Do NOT introduce new state libraries (Zustand, Redux, etc.). The existing React-state + Supabase pattern stays.

## Learnings

### Task 1 — Migration SQL
- The `update_updated_at_column()` trigger function is re-created with `CREATE OR REPLACE` to be idempotent (safe if 001 already ran it).
- No trigger on `rates` table by design — rate rows are immutable; changes = new INSERT, not UPDATE.
- `DROP TABLE IF EXISTS ... CASCADE` drops child FK rows cleanly without needing to order drops by dependency.
- The comment line "-- STEP 4: CREATE INDEXES" contains "CREATE INDEX" text and inflates grep -c counts by 1; actual index count was 8 as specified.

---

## Iteration 1 - Task 1: Write destructive migration SQL
- What was implemented: Created `supabase/migrations/003_v2_schema_reset.sql` with full v2 schema reset.
- Files changed: `supabase/migrations/003_v2_schema_reset.sql` (new), `PRD.md` (task marked complete), `progress.txt` (this update).
- Learnings for future iterations:
  - DROP order: time_entries → projects → clients (or use CASCADE on each DROP)
  - Re-declare trigger function with CREATE OR REPLACE for idempotency
  - No trigger on rates table — immutable by design
  - source_observation_id is UUID with NO FK — observations table comes in run #2
---

## Iteration 2 - Task 2: Write seed migration SQL
- What was implemented: Created `supabase/migrations/004_v2_seed_trails.sql` with 6 trail INSERTs (single multi-row INSERT statement) and 2 rate INSERTs via INSERT/SELECT targeting slugs 'bb' and 'evermore-equine'.
- Files changed: `supabase/migrations/004_v2_seed_trails.sql` (new), `PRD.md` (task marked complete), `progress.txt` (this update).
- Learnings for future iterations:
  - Use INSERT/SELECT with WHERE slug IN (...) to reference just-inserted trails for the rates seed — avoids hardcoding UUIDs.
  - Multi-row INSERT for trails keeps the file compact and readable.
  - display_order 1-6 matches the PRD table order.
---

## Roadmap (For Future Context, Not This Run)

### Ralph Run #2 — Capture Layer
Add the observation and journaling foundation:
- `observations` table (timestamped events, attributable to trail/project, with `review_state`)
- `journal_entries` table (one or more per day, `kind` field for reflection/intention/freeform)
- `open_loops` table (tagged items from journal entries that persist across days)
- Manual journal entry UI (the evening reflection / "trail report")
- Observation review queue UI (triage during evening reflection)
- Tag parser (extract `@tomorrow`, `#stuck`, `>trail-slug` from journal prose into `parsed_tags` JSONB)
- Add FK constraint from `time_entries.source_observation_id` to `observations.id`

### Ralph Run #3 — Briefing & Skill Ingestion
- Morning briefing view ("today's trail map") computed from journal + observations + open loops + active timers
- `POST /api/observe` endpoint for AI tool skills to submit observations
- Skill-to-time-entry propagation flow (manual confirmation for billable trails, optional auto for personal)
- DObot revival, eventual integration as briefing composer

### Ralph Run #4+ — Polish, Mobile, Edge Cases
- README rewrite reflecting full v2 product
- Per-entry billable override (`is_billable` on time entries)
- Possible migration of `parsed_tags` JSONB to normalized tags table if tag management UI is wanted
- Mobile polish

---
