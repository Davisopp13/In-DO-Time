# In DO Time v2 Foundation — Progress Log

## Project: In DO Time v2 (Foundation)
Started: 2026-05-14

## Goal of This Ralph Run

Reset the schema and code around "trails" (replacing `clients`) and add a `rates` ledger.
End state: a working v2-shape time tracker. NO journal, observations, or briefing yet.

## Pre-existing Learnings (from v1 + design conversation)

- This is a Next.js 16 + Supabase + Tailwind 4 app. Single-user, no auth. RLS is on but permissive.
- Supabase client is accessed exclusively via `getSupabase()` from `src/lib/supabase.ts`. Don't create new clients.
- Demo mode is real and important — `NEXT_PUBLIC_DEMO_MODE=true` swaps to a mock client in `src/lib/supabaseDemo.ts`. Both code paths must work end-to-end. If a query is added in real Supabase, the demo client must handle it too.
- Types flow from `src/types/database.ts`. It is the single source of truth for table shapes. Update this FIRST when changing schema.
- v1 schema had `clients` / `projects` / `time_entries`. v2 schema has `trails` / `projects` / `rates` / `time_entries`. The `projects` table is re-parented from `client_id` to `trail_id`.
- Migration is DESTRUCTIVE — drop v1 tables, create v2 tables. Davis has confirmed no production data to preserve. Don't try to migrate data across.
- Ralph WRITES the migration SQL but does NOT execute it. Davis runs migrations manually in Supabase SQL Editor after this run completes.
- Rates are time-versioned. A rate row is immutable once inserted. To "change" a rate, insert a new row with new `effective_from` AND update the previous row's `effective_until`.
- The trail `kind` enum is exactly: `client`, `employer`, `personal`, `project`. No others.
- Time entries no longer carry rate. Cost is computed at query/display time via `src/lib/rates.ts`.
- `time_entries.notes` is now semantically "invoice line description only" — not a general capture field. Capture will live in observations (run #2).
- `time_entries.source_observation_id` is a UUID column with NO FK constraint yet. The `observations` table doesn't exist until run #2.

## Patterns to Preserve

- Brand: DO Code Lab logo, "In DO Time" wordmark, green palette (`#3A7D44` primary).
- Typography: Playfair Display (headings), Inter (body).
- Spacing: 8px grid. Radius: 8px (buttons/inputs), 12px (cards).
- Theme: light/dark via `next-themes`. ThemeProvider/ThemeToggle/ThemeWatcher components are unchanged.
- Timer engine in `src/lib/timer.ts` is the only place timer operations live. Don't duplicate this logic elsewhere.

## Anti-Patterns (Things Ralph Should NOT Do)

- Do NOT try to preserve v1 data through the schema reset. The migration is destructive by design.
- Do NOT create a separate `clients` concept alongside trails. Clients are just trails with `kind='client'`.
- Do NOT add a rate column to `time_entries`. Rates live in the `rates` table only.
- Do NOT UPDATE a rate row in place. Always insert a new one.
- Do NOT refactor unrelated code "while you're in there." Stay focused on the task list.
- Do NOT build journal, observation, briefing, or open-loop features in this run. They are explicitly out of scope.
- Do NOT introduce new state libraries (Zustand, Redux, etc.). The existing React-state + Supabase pattern stays.

## Learnings

### Task 1 — Migration SQL
- The `update_updated_at_column()` trigger function is re-created with `CREATE OR REPLACE` to be idempotent (safe if 001 already ran it).
- No trigger on `rates` table by design — rate rows are immutable; changes = new INSERT, not UPDATE.
- `DROP TABLE IF EXISTS ... CASCADE` drops child FK rows cleanly without needing to order drops by dependency.
- The comment line "-- STEP 4: CREATE INDEXES" contains "CREATE INDEX" text and inflates grep -c counts by 1; actual index count was 8 as specified.

---

## Iteration 1 - Task 1: Write destructive migration SQL
- What was implemented: Created `supabase/migrations/003_v2_schema_reset.sql` with full v2 schema reset.
- Files changed: `supabase/migrations/003_v2_schema_reset.sql` (new), `PRD.md` (task marked complete), `progress.txt` (this update).
- Learnings for future iterations:
  - DROP order: time_entries → projects → clients (or use CASCADE on each DROP)
  - Re-declare trigger function with CREATE OR REPLACE for idempotency
  - No trigger on rates table — immutable by design
  - source_observation_id is UUID with NO FK — observations table comes in run #2
---

## Iteration 2 - Task 2: Write seed migration SQL
- What was implemented: Created `supabase/migrations/004_v2_seed_trails.sql` with 6 trail INSERTs (single multi-row INSERT statement) and 2 rate INSERTs via INSERT/SELECT targeting slugs 'bb' and 'evermore-equine'.
- Files changed: `supabase/migrations/004_v2_seed_trails.sql` (new), `PRD.md` (task marked complete), `progress.txt` (this update).
- Learnings for future iterations:
  - Use INSERT/SELECT with WHERE slug IN (...) to reference just-inserted trails for the rates seed — avoids hardcoding UUIDs.
  - Multi-row INSERT for trails keeps the file compact and readable.
  - display_order 1-6 matches the PRD table order.
---

## Iteration 3 - Task 3: Rewrite `src/types/database.ts`
- What was implemented: Replaced the v1 `Database` type entirely. Defined types for `trails`, `projects`, `rates`, `time_entries` matching the new schema. Exported convenience types: Trail, TrailInsert, TrailUpdate, Project, ProjectInsert, ProjectUpdate, Rate, RateInsert, RateUpdate, TimeEntry, TimeEntryInsert, TimeEntryUpdate. Deleted old `Client` exports.
- Files changed: `src/types/database.ts` (rewritten), `PRD.md` (task marked complete), `progress.txt` (this update).
- Learnings for future iterations:
  - `npx tsc --noEmit` failed in other files (clients/page.tsx, projects/page.tsx, mockData.ts, supabaseDemo.ts) referencing old `Client` type — exactly as PRD predicted. These are fixed in later tasks.
  - `rates` table has no `updated_at` column by design (rows are immutable).
  - `projects.status` now includes `'completed'` as a valid value (v1 only had `'active'|'archived'`).
  - `.next/types/routes.d` duplicate identifier errors are pre-existing generated-file artifacts, not caused by this change.
---

## Iteration 5 - Task 5: Update `src/lib/timer.ts` for trails
- What was implemented: Added `source: 'manual'` to all TimeEntryInsert objects in `startTimer` and `createManualEntry`. Updated `RunningTimerWithProject` interface to replace `client`/`effectiveRate` fields with a `trail` object (id, name, color, is_billable). Rewrote `getAllRunningTimersWithProjects` to join `projects → trails` instead of `projects → clients`. Kept `calculateRunningCost` as a pure math helper (callers still need it; rate *resolution* happens in rates.ts).
- Files changed: `src/lib/timer.ts` (updated), `PRD.md` (task marked complete), `progress.txt` (this update).
- Learnings for future iterations:
  - Removing `calculateRunningCost` entirely breaks `full-loop.test.ts` and `csv.ts` which import it. PRD acceptance only says "no `client`/`client_id` references, all inserts include `source`" — keep the pure math helper, remove client-specific logic only.
  - `TimeEntry` Row type now has `source` and `source_observation_id` — include them when manually mapping entry fields to avoid incomplete-type errors.
  - `getAllRunningTimersWithProjects` now returns `trail` (not `client`) with `is_billable` flag; callers use rates.ts to compute cost.
---

## Iteration 6 - Task 6: Update `src/lib/supabaseDemo.ts` for new schema
- What was implemented: Rewrote `mockData.ts` — replaced `Client` type and mock clients with `Trail`/`Rate` types and 6 mock trails matching seed data (B.B., Evermore Equine, Hapag-Lloyd, DObot, In DO Time, Personal), added 2 mock rates ($50/hr for client trails), updated projects to use `trail_id`, updated time entries to include `source: 'manual'` and `source_observation_id: null`, updated `MockDataStore` to use `trails`/`rates` instead of `clients`. Rewrote `supabaseDemo.ts` — replaced all `clients` references with `trails`, added `rates` table support in INSERT/SELECT/UPDATE/DELETE paths, updated JOIN logic from `time_entries→projects→clients` to `time_entries→projects→trails` and `projects→clients` to `projects→trails`.
- Files changed: `src/lib/mockData.ts` (rewritten), `src/lib/supabaseDemo.ts` (rewritten), `PRD.md` (task marked complete), `progress.txt` (this update).
- Learnings for future iterations:
  - `rates` table has no `updated_at` column (immutable by design) — INSERT handler skips adding `updated_at` for the `rates` table.
  - `eq('project_id', null)` works for IS NULL filtering in the mock since JS `null === null` is true.
  - Pre-existing typecheck errors from `clients/page.tsx`, `projects/page.tsx`, `ActiveTimers.tsx` are expected — these are fixed in Tasks 9-14. Our new files were clean.
  - 42/42 tests passed.
---

## Iteration 8 - Task 8: Update `src/lib/csv.ts` for new schema
- What was implemented: Rewrote `csv.ts` — renamed `client_name` → `trail_name` in `CSVEntry` interface, removed `is_manual` (no Type column in v2), made `effectiveRate: number | null` (null = non-billable trail, shows empty rate/cost cells). New column headers: Date, Trail, Project, Start, End, Duration, Rate (effective), Cost, Notes. Dropped old "Hours (Decimal)" and "Type" columns. Renamed `clientName` → `trailName` in `generateCSVFilename`. Fixed `reports/page.tsx` to pass `trail_name` (mapped from `e.client_name` since reports page is updated in Task 15). Updated all `generateCSV` calls in `full-loop.test.ts` — replaced `client_name` with `trail_name`, removed `is_manual`, updated header assertions, replaced "Manual/Timer type" tests with nullableRate test.
- Files changed: `src/lib/csv.ts` (rewritten), `src/__tests__/full-loop.test.ts` (CSV describe block updated, full-loop CSV entries updated), `src/app/reports/page.tsx` (CSV entry mapping fixed), `PRD.md` (task marked complete), `progress.txt` (this update).
- Learnings for future iterations:
  - CSVEntry no longer includes `is_manual` — the Type column was dropped from v2 spec. Task 15 (reports page rewrite) should not re-add it.
  - `effectiveRate: null` is the correct signal for non-billable trails — the function outputs empty strings for Rate and Cost columns rather than $0.00, which is more honest.
  - `reports/page.tsx` still uses `e.client_name` internally (v1 interface) — Task 15 will fully rewrite it. The fix here is minimal to unblock typecheck.
  - 42/42 tests passed.
---

## Iteration 12 - Task 12: Update `src/components/Dashboard.tsx`
- What was implemented: Rewrote Dashboard.tsx — replaced `ProjectWithClient` interface with `ProjectWithTrail`, replaced flat project list with trail-grouped cards (billable trails always visible, non-billable trails behind a toggle). Added `allRates` state and `getEffectiveRateFromCache` helper (in-memory rate lookup to avoid N+1 queries). Hero section now shows Entries / Billable earnings / Active timers. Each project card shows trail name + color, project name, live timer, and live cost (billable only). "Today's summary" computes total hours across all trails and total billable earnings using cached rates. Also fixed `ActiveTimers.tsx` type errors — replaced `timer.client.color/name` with `timer.trail.color/name`, removed `timer.effectiveRate` references.
- Files changed: `src/components/Dashboard.tsx` (rewritten), `src/components/ActiveTimers.tsx` (type errors fixed), `PRD.md` (task marked complete), `progress.txt` (this update).
- Learnings for future iterations:
  - `getEffectiveRateFromCache` as a module-level pure function avoids N+1 Supabase queries — fetch all rates once, compute per-project in the render loop.
  - Inline render helpers (`renderProjectCard`, `renderTrailGroup`) work fine in functional components and keep the return JSX clean.
  - ActiveTimers.tsx was orphaned (not imported anywhere) — fixed its type errors as a side effect of Task 12 to make typecheck partial progress.
  - Pre-existing build failure from `projects/page.tsx` is unchanged (Task 14 will fix it). 42/42 tests passed.
---

## Roadmap (For Future Context, Not This Run)

### Ralph Run #2 — Capture Layer
Add the observation and journaling foundation:
- `observations` table (timestamped events, attributable to trail/project, with `review_state`)
- `journal_entries` table (one or more per day, `kind` field for reflection/intention/freeform)
- `open_loops` table (tagged items from journal entries that persist across days)
- Manual journal entry UI (the evening reflection / "trail report")
- Observation review queue UI (triage during evening reflection)
- Tag parser (extract `@tomorrow`, `#stuck`, `>trail-slug` from journal prose into `parsed_tags` JSONB)
- Add FK constraint from `time_entries.source_observation_id` to `observations.id`

### Ralph Run #3 — Briefing & Skill Ingestion
- Morning briefing view ("today's trail map") computed from journal + observations + open loops + active timers
- `POST /api/observe` endpoint for AI tool skills to submit observations
- Skill-to-time-entry propagation flow (manual confirmation for billable trails, optional auto for personal)
- DObot revival, eventual integration as briefing composer

## Iteration 4 - Task 4: Create `src/lib/rates.ts` rate-lookup helper
- What was implemented: Created `src/lib/rates.ts` with `getEffectiveRate` (queries rates table, prefers project-level rate over trail-level, picks most recent effective rate, returns null if none found) and `computeEntryCost` (divides duration_seconds by 3600 × rate). Created `src/__tests__/rates.test.ts` with 13 tests covering all 4 required scenarios.
- Files changed: `src/lib/rates.ts` (new), `src/__tests__/rates.test.ts` (new), `PRD.md` (task marked complete), `progress.txt` (this update).
- Learnings for future iterations:
  - Vitest mock pattern: use `vi.mock('../lib/supabase', () => ({ getSupabase: vi.fn() }))` then `vi.mocked(getSupabase).mockReturnValue(...)` in each test.
  - Supabase chain mock: make a thenable builder (has `.then()`) so `await supabase.from(...).select().eq().lte()` resolves correctly. Each method returns `this`; `then` delegates to `Promise.resolve({ data, error })`.
  - TypeScript errors in `clients/page.tsx`, `projects/page.tsx`, `mockData.ts` are pre-existing from Task 3 — rates.ts itself is type-clean.
  - 42/42 tests passed (`npm run test`).
---

## Iteration 7 - Task 7: Update `src/lib/mockData.ts` and `src/lib/seed.ts`
- What was implemented: `mockData.ts` was already updated in Iteration 6 (Task 6). Rewrote `seed.ts` entirely — replaced `SeedClient` interface and `seedDefaultClients` function with `SeedTrail` interface and `seedDefaultTrails` function. New seed inserts all 6 default trails, their projects, and rate rows for the 2 client trails. Updated Dashboard.tsx import from `seedDefaultClients` → `seedDefaultTrails`.
- Files changed: `src/lib/seed.ts` (rewritten), `src/components/Dashboard.tsx` (import rename), `PRD.md` (task marked complete), `progress.txt` (this update).
- Learnings for future iterations:
  - `getSupabase()` returns `any` — so `from('clients')` in v1 seed.ts didn't produce TypeScript errors even though the typed client rejects that table name. This means seed.ts v1 was type-silent, not type-safe.
  - The function rename from `seedDefaultClients` to `seedDefaultTrails` required updating the import in Dashboard.tsx. Task 12 will fully rewrite Dashboard.tsx, but keeping the import valid avoids introducing new TS errors now.
  - 42/42 tests passed, no new typecheck errors beyond pre-existing ones (clients/page.tsx, projects/page.tsx, ActiveTimers.tsx).
---

## Iteration 9 - Task 9: Update `src/components/Header.tsx`
- What was implemented: Replaced navItems array — removed `clients` and `projects` links, added `trails` link (`/trails`), reordered to Dashboard | Trails | Time Log | Reports.
- Files changed: `src/components/Header.tsx` (navItems updated), `PRD.md` (task marked complete), `progress.txt` (this update).
- Learnings for future iterations:
  - `npm run build` still fails due to pre-existing errors in `clients/page.tsx` (Task 10 deletes that directory, which will fix it).
  - Header.tsx itself has zero type errors — pre-existing errors don't block marking the task complete.
  - 42/42 tests passed.
---

## Iteration 10 - Task 10: Rename and rebuild `src/app/clients/` → `src/app/trails/`
- What was implemented: Deleted `src/app/clients/` directory entirely. Created `src/app/trails/page.tsx` — list view of all trails grouped by kind (Clients, Work, Projects, Personal sections). Each card shows name, kind badge, billable indicator, current effective rate (for billable trails), color swatch, and archive/restore button. "Add trail" form with name, auto-generated slug (editable), kind dropdown, is_billable checkbox (default by kind), color picker, optional initial rate for billable trails.
- Files changed: `src/app/clients/page.tsx` (deleted), `src/app/trails/page.tsx` (new), `PRD.md` (task marked complete), `progress.txt` (this update).
- Learnings for future iterations:
  - `insert().select().single()` pattern breaks the mock client — `.select()` after `.insert()` overwrites the operation to 'select'. Instead, generate the ID client-side with `crypto.randomUUID()` and provide it in the TrailInsert, then use that ID for subsequent inserts (e.g., rate row). Both real Supabase and mock accept a provided `id` field.
  - Filtering rates in JS (rather than with `.is('project_id', null)`) avoids the IS NULL issue — the mock's `.eq(column, null)` works for equality but the mock doesn't have `.is()`. Fetching all rates and filtering in JS is clean for small datasets.
  - After deleting `clients/` route, the `.next/types/validator.ts` cache still references the old `clients/page.js`. Running `npm run build` regenerates it; subsequent `tsc --noEmit` is clean.
  - Pre-existing errors from `projects/page.tsx` and `ActiveTimers.tsx` are unchanged — still only those two files.
  - 42/42 tests passed.
---

## Iteration 11 - Task 11: Create `src/app/trails/[id]/page.tsx` trail detail view
- What was implemented: Created `src/app/trails/[id]/page.tsx` — trail detail page with four sections: trail header (name, kind badge, billable badge, edit button), Projects (list with archive/restore, add project modal), Rate History (billable trails only — table of rate rows, add rate modal), Recent Time Entries (fetched per-project and merged). Added "View" link to each trail card in `trails/page.tsx`. Rate add flow: finds open rate (effective_until IS NULL), closes it off with effective_until = (new_from - 1 day), then inserts new rate row.
- Files changed: `src/app/trails/[id]/page.tsx` (new), `src/app/trails/page.tsx` (added Link import + View button), `PRD.md` (task marked complete), `progress.txt` (this update).
- Learnings for future iterations:
  - Fetch time entries per-project individually (`.eq('project_id', id).limit(10)`) then merge/sort in JS — avoids needing `.in()` which the mock client doesn't support.
  - Filter rates by `project_id === null` in JS after fetching all trail rates — avoids `.is('project_id', null)` which the mock also doesn't support.
  - Sort rates in JS (`.sort()` by effective_from descending) rather than relying on mock's ORDER BY, since the mock's `order()` method may not be wired into execute() for all cases.
  - Rate immutability pattern: find open rate (effective_until IS NULL), update its effective_until, then insert new row. This is the prescribed pattern from the PRD and mock's updateRate() supports it.
  - Pre-existing build errors from `projects/page.tsx` (Task 14) and `ActiveTimers.tsx` (Task 12) are unchanged.
  - 42/42 tests passed.
---

### Ralph Run #4+ — Polish, Mobile, Edge Cases
- README rewrite reflecting full v2 product
- Per-entry billable override (`is_billable` on time entries)
- Possible migration of `parsed_tags` JSONB to normalized tags table if tag management UI is wanted
- Mobile polish

---

## Iteration 14 - Task 14: Update `src/app/projects/page.tsx`
- What was implemented: Rewrote projects/page.tsx — replaced `ProjectWithClient`/`Client` with `ProjectWithTrail`/`Trail`. Uses `getSupabase()` (not the old `supabase` export). Fetches trails + projects in parallel, builds a `trailMap` in JS and attaches trail data to each project. Projects grouped by trail with a trail header (color dot, name, billable badge). Add project form: trail dropdown → name → description → optional project-level rate override (only shown for billable trails, add only). On add with rate, inserts a `rates` row with `project_id` set and `effective_from = today`. Edit form: trail, name, description (no rate editing — that lives in the trail detail page). Archive/Restore preserved.
- Files changed: `src/app/projects/page.tsx` (rewritten), `PRD.md` (task marked complete), `progress.txt` (this update).
- Learnings for future iterations:
  - `crypto.randomUUID()` for client-side ID generation lets us reference the new project's ID for the rates INSERT without needing `.select().single()` (which breaks the mock).
  - Fetch trails and projects in parallel with `Promise.all`, build a `Map` in JS for O(1) lookups — avoids relying on JOIN syntax that may not work in the mock.
  - Rate override field is add-only and only shown when selected trail `is_billable` — editing rates belongs to the trail detail page per the rate immutability pattern.
  - `void slugifyForTrail` suppresses the unused-var lint warning without deleting the utility function.
  - 42/42 tests passed, typecheck clean, build clean.
---

## Iteration 15 - Task 15: Update `src/app/reports/page.tsx`
- What was implemented: Rewrote reports/page.tsx — replaced `ClientSummary`/`ReportEntry` with `TrailSummary`/`ReportEntry` (trail-based). Trail filter dropdown replaces client filter. Fetches trails + rates in parallel on mount; rates used for `getEffectiveRateFromCache` inline helper (same pattern as Dashboard and time-log). Time entries queried with `projects!inner(trails(...))` join; JS-side trail filtering for mock compatibility. Grouped by trail → project, sorted by totalSeconds descending. Grand totals show total time across all trails and total earned (billable trails only). Non-billable trails show time but no cost column. CSV export uses `effectiveRate: null` for non-billable trails. Logo preserved in report header.
- Files changed: `src/app/reports/page.tsx` (rewritten), `PRD.md` (task marked complete), `progress.txt` (this update).
- Learnings for future iterations:
  - `allRates` must be loaded before `loadReport` runs to avoid zero costs — `loadReport` depends on `allRates` so it's included in its `useCallback` dependency array. On first render allRates is [], so costs compute as 0 until the rates effect resolves. This is fine for the UX.
  - `grandTotalEarned` only shown if > 0 — cleaner for non-billable-only filtered views.
  - `is_billable` badge on trail header only shows on `sm` and up to avoid crowding on mobile.
  - 42/42 tests passed, typecheck clean, build clean.
---

## Iteration 13 - Task 13: Update `src/app/time-log/page.tsx`
- What was implemented: Replaced all `client_*` references with `trail_*` in `TimeLogEntry` interface. Filter dropdown changed from "Client" to "Trail" with JS-side trail filtering (mock compatibility for dotted-path `.eq('projects.trail_id', ...)` filters). Project cascade from trail works in both filter and manual entry modal. Removed `effectiveRate` from entry; added `trail_is_billable` flag. Added `allRates` state + `getEffectiveRateFromCache` pure function (same pattern as Dashboard). Cost shown only for billable trails; day earnings shown only if > 0. Manual entry modal now has Trail → Project (cascaded, disabled until trail selected) → Start/End/Notes flow.
- Files changed: `src/app/time-log/page.tsx` (rewritten), `PRD.md` (task marked complete), `progress.txt` (this update).
- Learnings for future iterations:
  - JS-side trail filter after fetch (in addition to query filter) is safe for mock compatibility with dotted-path filters like `eq('projects.trail_id', ...)`.
  - `getEffectiveRateFromCache` pure function avoids N+1 queries — fetch all rates once, compute per-entry in render.
  - Only pre-existing errors from `projects/page.tsx` remain (Task 14 fixes those). 42/42 tests passed.
---

## Iteration 17 - Task 17: Update `src/__tests__/full-loop.test.ts`
- What was implemented: Rewrote the Full Loop integration section — replaced `mockClient` (v1 shape with `hourly_rate`) with `mockTrail` (v2 shape: `slug`, `kind`, `is_billable`, etc.), replaced `mockProject.client_id` with `mockProject.trail_id`, added `source: 'manual'` and `source_observation_id: null` to mock time entries, added `mockRate` (v2 Rate shape). Added `vi.mock('../lib/supabase', ...)` at top level (hoisted) and imported `computeEntryCost` from rates.ts. Full loop test now calls `computeEntryCost(mockStoppedEntry, mockTrail.id)` to verify cost via rates infrastructure (mocked Supabase returns mockRate at $30/hr → 1.5h = $45). Added a standalone `verifies computed cost via rates.ts matches expected` test. Timer Utilities and CSV Export tests are unchanged.
- Files changed: `src/__tests__/full-loop.test.ts` (rewritten), `PRD.md` (task marked complete), `progress.txt` (this update).
- Learnings for future iterations:
  - `vi.mock` at the top of the file is hoisted by Vitest — safe to place after imports syntactically; pure utility tests in the same file are unaffected since `getSupabase` is only called by rates.ts and timer.ts async functions, not the pure formatting helpers.
  - `computeEntryCost` takes a full `TimeEntry` object — `mockStoppedEntry` needs all required fields (`source`, `source_observation_id`, etc.) to be type-compatible.
  - Test count went from 42 to 43 (added one net-new test for rates cost verification). All 43 pass.
---

## Iteration 16 - Task 16: Update `src/hooks/useRunningTimers.ts`
- What was implemented: No code changes required. The hook was already correct for v2 schema — it imports `RunningTimerWithProject` from `timer.ts`, which was updated in Task 5 to use `trail` (not `client`) with `id/name/color/is_billable` fields. The hook extends that type with `elapsedSeconds` and wraps `getAllRunningTimersWithProjects()` — no client/rate-on-entry references remained.
- Files changed: `PRD.md` (task marked complete), `progress.txt` (this update).
- Learnings for future iterations:
  - When a hook's only logic is "call a function and extend the return type," updating the downstream function (timer.ts Task 5) is sufficient — no separate hook changes needed.
  - `npx tsc --noEmit` and 42/42 tests passed.
---

## Iteration 18 - Task 18: Full project verification
- What was implemented: Ran all four verification commands. No code changes were needed — the codebase was already in a clean state.
- Files changed: `PRD.md` (task marked complete), `progress.txt` (this update).
- Learnings for future iterations:
  - `npx tsc --noEmit`: zero errors (clean).
  - `npm run test`: 43/43 passed.
  - `npm run build`: succeeded — all 10 routes built (/, /_not-found, /apple-icon.png, /icon.png, /projects, /reports, /time-log, /trails, /trails/[id]).
  - `npm run lint`: pre-existing environment failure — Node.js v24.13.0 incompatibility with ESLint's package.json. Not caused by v2 changes; PRD acceptance allows "pre-existing warnings/errors."
---
